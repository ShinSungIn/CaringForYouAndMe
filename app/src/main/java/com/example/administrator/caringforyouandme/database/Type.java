package com.example.administrator.caringforyouandme.database;

public class Type {
    public static String INTEGER = "integer"; // integer, int, tinyint, smallint, mediumint, bigint, unsignedint, int2, int8
    public static String NUMERIC = "numeric"; // numeric, decimal(10, 5), boolean, date, datetime
    public static String TEXT = "text"; // text, character(20), varchar(255), varying character(255), nchar55), native character(70), nvarchar(100), clob
    public static String REAL = "real"; // real, double, double precision, float
    public static String BLOB = "blob"; // blob
}
